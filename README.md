# StrmTool Showcase

这是 StrmTool 的项目介绍站，用于展示本地媒体库扫描、识别、链接和自动监控能力。

站点是无构建依赖的静态页面，入口为根目录的 `index.html`。推送到 `main` 后，GitHub Actions 会自动部署到 GitHub Pages。

在线地址：<https://sisheng36.club/strmtool-web/>

本地预览：

```bash
python3 -m http.server 8000
```

然后打开 <http://localhost:8000>。
